#!/bin/sh -eux

DIR=$(dirname $0)
. "${DIR}/../../setup.sh"

$IVORY create-repository "${REPOSITORY}"
$IVORY import-dictionary -p "${INPUT}/dictionary.psv"
$IVORY ingest -z "Australia/Sydney" -i "${INPUT}/facts"
$IVORY recreate -i "${REPOSITORY}" -o "${TARGET}/out" -t factset

### check factsets ###
$IVORY cat-facts "${TARGET}/out/factsets/00000/halibut/2014/*/*/*" | sort > "${TARGET}/facts.psv"
diff_test "${DIR}/expected/halibut-facts.psv" "${TARGET}/facts.psv"
